## Run
No dependencies — Python 3 + tkinter (standard library) only.

## Features

- Auto-detects your TF2 `tf/custom` folder via Steam
- Add / enable / disable / remove `.vpk` files
- Detects mod type

Not affiliated with Valve.
